{
	"builds": [
		{
			"src": "frontend/package.json",
			"use": "@vercel/static-build",
			"config": { "distDir": "/" }
		}
	],
	"rewrites": [{ "source": "/(.*)", "destination": "/dist/index.html" }]
}
