# Docker compose file for building necessary images (and running them)
services:
  # Step 1: Build backend api server
  backend:
    build:
      context: .
      dockerfile: Dockerfile
      target: backend
    volumes:
      - ./backend:/backend
    networks:
      - app-network
    image: template-backend:1.0.0

  # Step 2: Compile frontend static files
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: frontend
    volumes:
      - ./frontend:/frontend
    networks:
      - app-network
    depends_on:
      - backend
    image: template-frontend:1.0.0

  # Step 3: Build nginx server
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
      target: nginx
    ports:
      - "80:80"
      - "443:443"
    networks:
      - app-network
    depends_on:
      - backend
      - frontend
    image: template-nginx:1.0.0

networks:
  app-network:
    driver: bridge