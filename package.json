{
	"name": "template",
	"version": "1.0.0",
	"description": "",
	"license": "ISC",
	"author": "Hoo Di Heng",
	"type": "commonjs",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "docker-compose -f docker-compose-dev.yaml up --build -d",
		"dev": "concurrently \"cd ./backend && npm run dev\" \"cd ./frontend && npm run dev\"",
		"build": "docker-compose -f docker-compose-dev.yaml up --build",
		"clear-images": "for /f \"tokens=*\" %i in ('docker images -q') do docker rmi -f %i",
		"clear-containers": "for /f \"tokens=*\" %i in ('docker ps -q') do docker rm -f %i",
		"ssh": "ssh root@138.197.85.71"
	}
}
